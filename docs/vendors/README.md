# Vendors Docs Sync (Unified Interface)

- 共通コマンド
  - `pnpm docs:vendors:sync` … 仕様の取得
  - `pnpm docs:vendors:build` … 型/Zod/HTML/Markdown 生成
  - `pnpm docs:vendors:all` … 上記まとめて
- 対象: `square`, `fedex`（`VENDORS` 環境変数で制御）
- いずれかのベンダが未実装でも **自動スキップ** します。


