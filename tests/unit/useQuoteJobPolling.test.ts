import { describe, it, expect } from 'vitest'

describe('useQuoteJobPolling', () => {
  it('switches to completed when API returns completed', async () => {
    expect(true).toBe(true)
  })

  it('timeouts after 30s', async () => {
    expect(true).toBe(true)
  })
})


